{
  "name": "node-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon -L src/index.js",
    "restart-container": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
    "rebuild-container": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build",
    "stop-container": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.8.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
